import styles from './index.module.scss';
import { Section } from '../Section';
import { HeadingTitle } from '@/components/elements/HeadingTitle';

export function Contact() {
  return (
    <Section>
      <HeadingTitle title="Contact" align="center" />
    </Section>
  );
}
